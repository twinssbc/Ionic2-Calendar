import { OnInit, OnChanges, EventEmitter, ElementRef } from '@angular/core';
import { CalendarService } from "./calendar.service";
export declare class CalendarComponent implements OnInit, OnChanges {
    private elementRef;
    private calendarService;
    eventSource: any;
    calendarMode: String;
    currentDate: Date;
    currentDateChange: EventEmitter<{}>;
    onRangeChanged: EventEmitter<{}>;
    onEventSelected: EventEmitter<{}>;
    onTimeSelected: EventEmitter<{}>;
    onTitleChanged: EventEmitter<string>;
    formatDay: String;
    formatDayHeader: String;
    formatDayTitle: String;
    formatWeekTitle: String;
    formatMonthTitle: String;
    formatWeekViewDayHeader: String;
    formatHourColumn: String;
    showEventDetail: Boolean;
    startingDayMonth: number;
    startingDayWeek: number;
    allDayLabel: String;
    noEventsLabel: String;
    queryMode: String;
    step: any;
    hourParts: number;
    inited: boolean;
    constructor(elementRef: ElementRef, calendarService: CalendarService);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    rangeChanged(event: any): void;
    eventSelected(event: any): void;
    timeSelected(event: any): void;
    titleChanged(title: any): void;
    private setAttributeValue(nativeElement, attributeName);
}
